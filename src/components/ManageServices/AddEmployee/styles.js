const styles = (theme) => ({
  formContainer: {
    display: 'flex',
    justifyContent: 'center',
    padding: 10,
    flexDirection: 'column',
  },
  button: {
    marginTop: 20,
  },
  menuPaper: {
    maxHeight: 300,
  },
  alert: {
    marginBottom: theme.spacing(2),
    maxWidth: '400px',
  },
});

export default styles;
